{
  "name": "@mfe-practical/catalog",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/catalog/src",
  "projectType": "application",
  "tags": ["scope:app", "type:feature"],
  "// targets": "to see all targets run: nx show project @mfe-practical/catalog --web",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/catalog",
        "command": "rspack serve --node-env=development --port=4201"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config apps/catalog/vitest.config.ts --passWithNoTests"
      }
    }
  }
}
