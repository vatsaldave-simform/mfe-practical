{
  "name": "@mfe-practical/checkout",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/checkout/src",
  "projectType": "application",
  "tags": ["scope:app", "type:feature"],
  "// targets": "to see all targets run: nx show project @mfe-practical/checkout --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config apps/checkout/vitest.config.ts --passWithNoTests"
      }
    }
  }
}
